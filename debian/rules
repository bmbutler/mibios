#!/usr/bin/make -f
export DH_VERBOSE = 1
export PYBUILD_NAME=hmb
export PYBUILD_SYSTEM=distutils
export PYBUILD_INSTALL_ARGS=--install-lib=/usr/lib/python3/dist-packages/
export PYTHONDONTWRITEBYTECODE=true

%:
	dh $@ --with python3 --buildsystem=pybuild
